// array

const frutas = ["manzana", "peras", "platanos"];

console.log(typeof frutas); //return object

frutas.push("cerezas");

console.log(frutas);

frutas.map((fruta, index) => {
  console.log(fruta);
});
console.log(frutas.includes("sandia"));

console.log(frutas.indexOf("manzana"));
frutas.sh;

// unshif: agrega un elemento al princio
// push : agrega un elemento al final

// shift : elimina el primer elemento del array
// pop : elimina el ultimo elemento

// includes : verifica si un elemento está en el array
// map : recorre el array
// indexOf : regrese el indice del elemento
